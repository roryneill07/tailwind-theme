{% comment %}
  TODO
  - Pagination
{% endcomment %}

<div class="container">
  <h1 class="font-bold text-4xl py-3">{{ blog.title }}</h1>
  <ul>
    {% for item in blog.articles %}
      <li>
        {{ item.title }}
      </li>
    {% endfor %}
  </ul>
</div>
